const Scrollbar = ({ data }) => {
    return (
        <div className = "container-scrollbar">
            <span>
                <img src = {data} alt="" />
            </span>
        </div>
    )
}

export default Scrollbar